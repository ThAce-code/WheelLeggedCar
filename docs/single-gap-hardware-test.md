# 单间隙视觉 + DL1B 硬件验收记录

日期：2026-07-16

分支：`codex/cone-perception-gap-mapping`

状态：软件门禁已通过；尚未测量轮周，尚未启用感知或运动。

## 1. 当前安全配置

| 配置 | 当前值 | 说明 |
|---|---:|---|
| `SINGLE_GAP_ENABLE` | `0U` | 不初始化 DL1B，不发布单间隙导航命令 |
| `SINGLE_GAP_MOTION_ENABLE` | `0U` | 无视觉运动权限 |
| `SINGLE_GAP_WHEEL_CIRCUMFERENCE_MM` | `0U` | 等待负载状态实测，禁止猜值 |
| `SINGLE_GAP_TOF_STOP_MM` | `350U` | 近距紧急停止阈值 |

在轮周测量、静态感知、架空轮测试逐项签字前，不得把运动开关改为 `1U`。

## 2. 软件门禁结果

以下测试在当前提交上全部通过：

- DL1B 失效闭锁主机测试
- 有界锥桶检测器主机测试
- 单间隙控制器主机测试
- CM7_0 轮速里程计位姿源主机测试
- CM7_1 应用四种构建测试：功能关闭、仅感知、RPM 超限、运动启用模拟
- 核间控制策略测试
- 单间隙静态集成与预处理配置门禁
- 相机/Seekfree、GNSS、UART 所有权和感知核间通信相关回归

## 3. IAR 三核构建结果

构建命令：`D:\IAR\common\bin\IarBuild.exe <project> -build Debug`。注册表安装项为 EWARM 9.40.1，命令行工具自身报告 `V9.2.2.10770`。

| 核 | 只读代码 | 只读数据 | 读写数据 | 错误 | 警告 |
|---|---:|---:|---:|---:|---:|
| CM0+ | 7,422 B | 526 B | 3,033 B | 0 | 0 |
| CM7_0 | 62,840 B | 2,085 B | 36,429 B（另有 22,564 B absolute） | 0 | 3 |
| CM7_1 | 30,804 B | 2,337 B | 13,971 B | 0 | 0 |

CM7_0 的 3 个警告均为 `control_leg.c` 中已有的命令变量“set but never used”。三核 map 均给出共享 SRAM 基址 `0x28080000`、大小 `0x2000`，即 8192 字节。

## 4. 轮周测量门禁（电机保持关闭）

给驱动轮和地面做对齐标记，在整车负载状态下人工滚动恰好 10 圈。每次重新对齐并独立测量。

| 次数 | 10 圈行程（m） | 单圈周长（mm） |
|---:|---:|---:|
| 1 |  |  |
| 2 |  |  |
| 3 |  |  |

计算：

```text
circumference_mm = round(1000 * median(travel_1_m, travel_2_m, travel_3_m) / 10)
```

- 三次单圈周长最大差异 / 中位数：____ %（必须不超过 1%）
- 写入的 `SINGLE_GAP_WHEEL_CIRCUMFERENCE_MM`：____ mm
- 测量人：____
- 复核人：____

## 5. 仅感知门禁

配置要求：轮周已写入，`SINGLE_GAP_ENABLE=1U`，`SINGLE_GAP_MOTION_ENABLE=0U`。重新构建并烧录受影响核心。

### 5.1 DL1B 静态距离

| 标称距离 | 实测距离 | DL1B 读数 | 期望结果 | 实际结果 | 通过 |
|---:|---:|---:|---|---|:---:|
| 0.20 m |  |  | `NAVIGATION_STOP_EMERGENCY` |  |  |
| 0.35 m |  |  | `NAVIGATION_STOP_EMERGENCY` |  |  |
| 0.50 m |  |  | 非近距停止 |  |  |
| 1.00 m |  |  | 非近距停止 |  |  |
| 2.00 m |  |  | 非近距停止 |  |  |
| 断开模块 | — | — | `NAVIGATION_STOP_INVALID` |  |  |
| 数据超过 100 ms 未更新 | — | — | `NAVIGATION_STOP_STALE` |  |  |

### 5.2 静态视觉

- 两锥桶有效帧数量：____（至少 20）
- 间隙中心最小值：____ px
- 间隙中心最大值：____ px
- 峰峰抖动：____ px（必须不超过 6 px）
- 加入第三个锥桶后是否报告歧义：____
- 全程导航命令 `enable` 是否保持 0：____
- 测试人/日期：____

## 6. 架空轮运动门禁

前置条件：底盘可靠支撑、车轮不接触地面、WiFi 图传已由配置自动关闭、操作者另行发送 `ARM`。

| 项目 | 期望 | 实际 | 通过 |
|---|---|---|:---:|
| 间隙右移 | 产生右修正 |  |  |
| 间隙左移 | 产生左修正 |  |  |
| 中心误差在死区内 | 转向为 0 |  |  |
| DL1B ≤ 350 mm | 100 ms 内停机 |  |  |
| DL1B 断开/无效 | 100 ms 内停机 |  |  |
| 图像陈旧 | 100 ms 内停机 |  |  |
| 里程计无效 | 停机 |  |  |
| 本地未 `ARM` | 不接受远端运动 |  |  |

若任何方向符号错误或停机门禁失败，立即恢复 `SINGLE_GAP_MOTION_ENABLE=0U` 后再分析。

测试人：____  复核人：____  日期：____

## 7. 低速地面验收

前置条件：开阔场地、宽间隙、专人持物理断电开关。

- 稳态速度：____ m/s（目标 `0.20 m/s ±10%`）
- 平面障碍插入时 350 mm ToF 停机：____
- 连续无碰撞、无人工转向、无意外续行次数：____ / 10
- 过门线后停止距离最大值：____ m（必须不超过 0.50 m）
- 是否保持目标速度不高于 0.20 m/s：____

测试人：____  安全员：____  复核人：____  日期：____

## 8. 最终结论

- [ ] 轮周重复性合格
- [ ] DL1B 距离与故障注入合格
- [ ] 静态视觉抖动与歧义检测合格
- [ ] 架空轮方向与停机门禁合格
- [ ] 10 次地面验收合格
- [ ] 所有证据已复核

结论：____

签字：____

日期：____
